# Assignment traces

## Base queries (A–H)

Run `./scripts/run_all_queries.sh` and copy `logs/agent7-run-*.log` excerpts into your submission.

## Custom queries (Part 2)

Generate with:

```bash
./scripts/run_custom_rag_traces.sh
```

Produces:

- `custom-q1-with-index.txt` / `custom-q1-no-index.txt`
- … through `q5`

Semantic queries: **q1**, **q2**, **q3** (see `jobs_rag_queries.py`).
